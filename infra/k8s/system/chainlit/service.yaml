apiVersion: v1
kind: Service
metadata:
  name: chainlit
  namespace: null-realm
  labels:
    app: chainlit
spec:
  type: NodePort
  selector:
    app: chainlit
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30001
      protocol: TCP
