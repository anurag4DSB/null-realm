# Placeholder — fill in real values before applying.
# Generate cookie-secret: python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secrets
  namespace: null-realm
type: Opaque
stringData:
  client-id: "REPLACE_WITH_OAUTH2_CLIENT_ID"
  client-secret: "REPLACE_WITH_OAUTH2_CLIENT_SECRET"
  cookie-secret: "REPLACE_WITH_COOKIE_SECRET"
