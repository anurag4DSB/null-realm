---
phase: 06-model-comparison-eval
type: execute
---

<objective>
Configure multiple LLM providers in LiteLLM, build comparison workflow, and create Chainlit comparison view.

Purpose: The core differentiator — run same task on multiple models, score side-by-side, pick the winner.
Output: Claude vs Gemini comparison in Chainlit with LLM-judge scores, costs, and human feedback.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/05-context-engineering/05-02-SUMMARY.md
@AGENT_TECH_LAB_PLAN.md (model comparison section)
@nullrealm/orchestrator/argo_client.py
@infra/k8s/system/litellm/litellm-config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add Gemini as second model (decision already made: Claude + Gemini)</name>
  <action>
  DECIDED in Phase 01: Claude (Anthropic) is primary since Phase 02. This phase adds Gemini as the comparison model.
  - Get GOOGLE_API_KEY from https://aistudio.google.com/apikey
  - Add gemini-pro model alias to LiteLLM config
  - Store GOOGLE_API_KEY in Secret Manager and K8s secret
  </action>
  <done>LiteLLM returns both claude-sonnet and gemini-pro in /v1/models</done>
</task>

<task type="auto">
  <name>Task 1: Multi-model LiteLLM config + comparison engine</name>
  <files>infra/k8s/system/litellm/litellm-config.yaml, nullrealm/orchestrator/comparison.py, nullrealm/eval/judge.py, nullrealm/eval/comparison.py</files>
  <action>
  Update LiteLLM config with selected models:
  - Add model aliases for each provider (based on decision above)
  - Ensure cost tracking is enabled per model

  Build comparison engine:
  - nullrealm/orchestrator/comparison.py:
    - ComparisonRunner class
    - run_comparison(task, models: list[str], session_id) → ComparisonResult
    - For each model: submit Argo workflow step in PARALLEL
    - Collect results, pass to judge

  Build LLM-as-judge:
  - nullrealm/eval/judge.py:
    - LLMJudge class
    - judge(task, result_a, result_b, model_a, model_b) → JudgeVerdict
    - Scores: correctness (1-5), completeness (1-5), code_quality (1-5), efficiency (1-5), overall (1-5)
    - Returns: scores + reasoning text
    - Use strongest available model as judge

  - nullrealm/eval/comparison.py:
    - Store results in evaluations table
    - Query comparison history: avg scores, costs, latency per model
  </action>
  <verify>python -c "from nullrealm.orchestrator.comparison import ComparisonRunner; print('ok')" && curl localhost:8000/api/v1/registry/tools | python -m json.tool</verify>
  <done>Comparison runner executes parallel model runs, judge scores results, data stored in evaluations table</done>
</task>

<task type="auto">
  <name>Task 2: Chainlit comparison view + human feedback</name>
  <files>ui/comparison_view.py, ui/app.py, nullrealm/api/routes/compare.py, nullrealm/eval/feedback.py</files>
  <action>
  Build comparison UI in Chainlit:
  - ui/comparison_view.py:
    - Side-by-side display: Model A output vs Model B output
    - Scores: correctness, completeness, code quality, efficiency, overall
    - Cost and latency per model
    - Judge's reasoning text
    - Human feedback buttons: Prefer Left / Prefer Right / Tie

  - nullrealm/api/routes/compare.py:
    - POST /api/v1/compare: trigger comparison run
    - GET /api/v1/compare/{run_id}: get comparison results
    - POST /api/v1/compare/{run_id}/feedback: record human preference

  - nullrealm/eval/feedback.py:
    - Store human feedback in evaluations table (evaluator_type="human")

  Wire into Chainlit:
  - Add comparison mode: user can prefix message with "/compare" to trigger
  - Results displayed in comparison view
  </action>
  <verify>In Chainlit: "/compare Research OAuth patterns" → see side-by-side results with scores</verify>
  <done>Comparison view shows side-by-side model outputs with scores, human can vote, data persisted</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Model comparison system</what-built>
  <how-to-verify>
    1. In Chainlit, type: "/compare What is Kubernetes?"
    2. See two model results side-by-side with scores
    3. Click "Prefer Left" or "Prefer Right" — feedback should persist
    4. Check Langfuse — should see traces for both model runs + judge
    5. Check evaluations table — scores and feedback stored
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Multiple models configured in LiteLLM
- [ ] Comparison runner executes parallel model runs
- [ ] LLM judge scores both results
- [ ] Chainlit shows side-by-side comparison view
- [ ] Human feedback stored in evaluations table
- [ ] Cost tracking per model, per run
</verification>

<success_criteria>
- Same task runs on 2+ models in parallel
- LLM judge produces meaningful scores with reasoning
- Chainlit comparison view is clear and actionable
- Human feedback loop works (prefer left/right/tie)
- Data-driven model selection becomes possible
</success_criteria>

<output>
After completion, create `.planning/phases/06-model-comparison-eval/06-01-SUMMARY.md`
</output>
