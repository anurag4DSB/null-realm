---
phase: 06-model-comparison-eval
type: execute
---

<objective>
Build golden test framework and Grafana dashboards for model comparison metrics.

Purpose: Systematic evaluation — not just ad-hoc comparisons, but repeatable test suites with trend dashboards.
Output: Golden test runner, model comparison leaderboard in Grafana, cost/latency dashboards.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/06-model-comparison-eval/06-01-SUMMARY.md
@AGENT_TECH_LAB_PLAN.md (golden test sets section)
@nullrealm/eval/judge.py
@nullrealm/eval/comparison.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden test framework</name>
  <files>eval/golden_tests/*.yaml, nullrealm/eval/golden_tests.py, scripts/run-golden-tests.sh</files>
  <action>
  Build golden test infrastructure:
  - eval/golden_tests/ directory with YAML test cases:
    - research_oauth.yaml: test research quality on OAuth topic
    - implement_endpoint.yaml: test code generation for a FastAPI endpoint
    - code_review.yaml: test review quality on intentionally buggy code
    Each test case has: id, category, difficulty, prompt, expected_behaviors, anti_patterns, models_to_compare, max_cost_usd

  - nullrealm/eval/golden_tests.py:
    - GoldenTestRunner class
    - load_tests(directory) → list of test cases
    - run_test(test_case, models) → comparison results
    - run_suite(directory, models) → full report with pass/fail per test
    - Output: JSON report with per-test, per-model scores

  - scripts/run-golden-tests.sh: CLI wrapper
    - Usage: ./scripts/run-golden-tests.sh --models gemini-pro,claude-sonnet --tests eval/golden_tests/
    - Outputs: JSON report + summary table

  Create at least 3 golden test cases covering different agent capabilities.
  </action>
  <verify>./scripts/run-golden-tests.sh --models gemini-pro --tests eval/golden_tests/ --dry-run</verify>
  <done>Golden test framework loads tests, runs them, produces JSON report with scores</done>
</task>

<task type="auto">
  <name>Task 2: Prometheus metrics + Grafana dashboards</name>
  <files>nullrealm/observability/metrics.py, nullrealm/observability/cost_tracker.py</files>
  <action>
  Add custom Prometheus metrics:
  - nullrealm/observability/metrics.py:
    - agent_request_duration_seconds (histogram): latency per agent, per model
    - agent_tokens_total (counter): input/output tokens per model
    - agent_cost_usd (counter): cost per model, per agent
    - agent_tool_calls_total (counter): tool usage per agent
    - workflow_step_duration_seconds (histogram): per-step latency
    - model_comparison_score (gauge): latest scores per model

  - nullrealm/observability/cost_tracker.py:
    - Track cost per request using LiteLLM's cost callbacks
    - Aggregate by: model, agent, session, workflow

  Create Grafana dashboards (JSON provisioning):
  - Model Comparison Leaderboard: avg scores, cost efficiency, latency by model
  - Agent Performance: request rate, latency p50/p95/p99, error rate
  - Cost Tracking: cost per model over time, cost per workflow, daily/weekly totals
  - K8s Resources: pod CPU/memory, NATS queue depth, DB connections

  Provision dashboards via ConfigMap in Grafana deployment.
  </action>
  <verify>curl localhost:8000/metrics | grep agent_ && curl localhost:3000/api/dashboards | python -m json.tool</verify>
  <done>Prometheus scraping custom metrics, Grafana shows 4 dashboards with real data</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Golden tests + monitoring dashboards</what-built>
  <how-to-verify>
    1. Run: ./scripts/run-golden-tests.sh --models gemini-pro --tests eval/golden_tests/
    2. Review JSON report — should show per-test scores
    3. Open Grafana (localhost:3000) — should see 4 new dashboards
    4. Model Comparison Leaderboard should show data from comparison runs
    5. Cost Tracking should show per-model cost breakdown
    6. Agent Performance should show request latency metrics
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 3+ golden test cases created and runnable
- [ ] Golden test runner produces JSON report
- [ ] Prometheus endpoint exposes custom metrics
- [ ] Grafana has 4 provisioned dashboards
- [ ] Dashboards show real data from agent executions
</verification>

<success_criteria>
- Golden test framework enables repeatable, automated model evaluation
- Grafana dashboards provide at-a-glance model comparison
- Cost tracking enables data-driven model selection
- Full observability stack: Langfuse (LLM traces) + Grafana (metrics) + Jaeger (spans)
</success_criteria>

<output>
After completion, create `.planning/phases/06-model-comparison-eval/06-02-SUMMARY.md`

This completes v1.0 of Null Realm. Consider marking milestone: v1.0 shipped.
</output>
