---
phase: 05-context-engineering
type: execute
---

<objective>
Build multi-user repo management: Argo-based indexing, repos table, GitHub PAT auth, Neo4j repo namespacing.

Purpose: Multiple users can index open + closed source repos via MCP. Indexing runs as Argo workflows (doesn't block MCP server). Private repos use GitHub PAT. Foundation for webhook re-indexing.
Output: `index_repo` triggers Argo workflow, `list_repos` shows status from repos table, private repos work via PAT.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/05-context-engineering/05-02-SUMMARY.md
@nullrealm/context/repo_manager.py
@nullrealm/mcp_server.py
@nullrealm/orchestrator/argo_client.py
@infra/k8s/argo-templates/agent-worker.yaml
@docs/architecture/decisions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Repository model + repos table</name>
  <files>nullrealm/registry/models.py</files>
  <action>
  Add Repository SQLAlchemy model to existing models.py:
  - name (unique), url, branch, auth_type (public/token), status (pending/indexing/ready/failed)
  - chunk_count, file_count, last_indexed_at, index_error
  - Auto-created by init_db() via Base.metadata.create_all
  </action>
  <done>repos table exists in PostgreSQL</done>
</task>

<task type="auto">
  <name>Task 2: Create Argo repo-indexer WorkflowTemplate</name>
  <files>infra/k8s/argo-templates/repo-indexer.yaml, nullrealm/context/repo_manager_cli.py, Dockerfile.worker</files>
  <action>
  Argo WorkflowTemplate for indexing repos in dedicated pods:
  - Image: null-realm-worker (add git + openssh-client to Dockerfile.worker)
  - Params: url, branch, repo_name, auth_type
  - Env: DATABASE_URL, NEO4J_URI, LITELLM_URL, GITHUB_TOKEN (from mcp-secrets)
  - Command: uv run python -m nullrealm.context.repo_manager_cli
  - Resources: 1Gi memory, 500m CPU

  Create repo_manager_cli.py as Argo pod entrypoint:
  - Parse args (url, branch, name, auth_type)
  - Update repos table status=indexing
  - Call index_repository()
  - Update repos table status=ready (or failed)
  </action>
  <done>Argo workflow template applied, worker image has git</done>
</task>

<task type="auto">
  <name>Task 3: Update repo_manager.py with repos table + PAT auth</name>
  <files>nullrealm/context/repo_manager.py</files>
  <action>
  Update existing repo_manager.py:
  - clone_or_pull(): support auth_type="token" → rewrite URL with GITHUB_TOKEN env var
  - index_repository(): upsert into repos table with status tracking
  - delete_repository_index(): also delete from repos table
  - list_indexed_repos(): query repos table (not pgvector GROUP BY)
  - New: register_repo(), update_repo_status(), get_repo()
  </action>
  <done>Repo management functions use repos table, PAT auth works</done>
</task>

<task type="auto">
  <name>Task 4: Update MCP tools to use Argo + repos table</name>
  <files>nullrealm/mcp_server.py</files>
  <action>
  Update index_repo MCP tool:
  - Register repo in repos table (status=pending)
  - Submit Argo workflow via ArgoClient.submit_workflow("repo-indexer", params)
  - Return "Indexing started, workflow: repo-index-xyz"
  - Do NOT wait for completion (async)

  Update list_repos: show status, URL, branch from repos table
  Update delete_repo_index: also delete repos table row
  New tool: add_repo(url, branch, auth_type) — register without indexing
  </action>
  <done>index_repo triggers Argo workflow, list_repos shows rich status</done>
</task>

<task type="auto">
  <name>Task 5: Add repo property to Neo4j nodes</name>
  <files>nullrealm/context/neo4j_store.py</files>
  <action>
  Add repo property to Symbol nodes:
  - MERGE (a:Symbol {file, name, repo}) — add repo param
  - New: delete_by_repo(repo_name) → MATCH (n:Symbol {repo}) DETACH DELETE n
  - Update delete_repository_index() to use clean repo-based deletion
  </action>
  <done>Neo4j nodes have repo property, per-repo deletion works</done>
</task>

<task type="auto">
  <name>Task 6: Deploy + test with public repo</name>
  <files>infra/k8s/gke/mcp/deployment.yaml</files>
  <action>
  - Add GITHUB_TOKEN to mcp-secrets K8s secret
  - Rebuild worker image (with git) and push to Artifact Registry
  - Rebuild MCP image and push
  - Apply Argo repo-indexer template on GKE
  - Restart MCP server
  - Test: index_repo("https://github.com/scality/cloudserver")
  - Verify: Argo UI shows workflow, list_repos shows status, code_search finds code
  </action>
  <done>Public repo indexed via Argo workflow, searchable via MCP tools</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Multi-user repo management with Argo-based indexing</what-built>
  <how-to-verify>
    1. list_repos() → "No repositories indexed"
    2. index_repo("https://github.com/scality/cloudserver") → "Indexing started, workflow: ..."
    3. Argo UI (argo.34.53.165.155.nip.io) → see indexer workflow running
    4. Wait 3-5 min, list_repos() → "cloudserver: status=ready, N chunks"
    5. code_search("S3 bucket") → returns code from cloudserver
    6. delete_repo_index("cloudserver") → clean removal
    7. For private repo: index_repo("https://github.com/scality/private-repo", auth_type="token")
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] repos table exists with correct schema
- [ ] Argo repo-indexer template created and applied
- [ ] Worker image has git installed
- [ ] index_repo triggers Argo workflow (async, returns immediately)
- [ ] list_repos shows status from repos table
- [ ] delete_repo_index cleans all stores + repos table
- [ ] Public repo indexes successfully
- [ ] Private repo indexes with GITHUB_TOKEN
- [ ] Neo4j nodes have repo property
- [ ] Multiple simultaneous index jobs work (different Argo pods)
</verification>

<success_criteria>
- MCP server stays lightweight (no git, no ML deps)
- Indexing runs in dedicated Argo pods (scalable, multi-user)
- repos table tracks all metadata (URL, branch, auth, status, stats)
- GitHub PAT enables private repo access
- Foundation ready for webhook-triggered re-indexing
</success_criteria>

<output>
After completion, create `.planning/phases/05-context-engineering/05-03-SUMMARY.md`
</output>
