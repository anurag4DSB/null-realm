---
phase: 03-streaming-persistence
type: execute
---

<objective>
Set up PostgreSQL with pgvector, create all 4 registry tables, build CRUD API, and seed initial data.

Purpose: Agents need configuration from registries. This is the persistence backbone for tools, prompts, assistants, and workflows.
Output: PostgreSQL running, 4 registry tables populated, CRUD API at /api/v1/registry/*.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/03-streaming-persistence/03-01-SUMMARY.md
@AGENT_TECH_LAB_PLAN.md (registry schemas section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database schema + SQLAlchemy models</name>
  <files>nullrealm/registry/models.py, nullrealm/registry/schemas.py, nullrealm/registry/database.py</files>
  <action>
  Use the existing PostgreSQL deployed in 01-02 (shared with Langfuse) or create app-specific DB.

  Create SQLAlchemy models matching the schema from AGENT_TECH_LAB_PLAN.md:
  - nullrealm/registry/database.py: async engine + session factory (asyncpg)
  - nullrealm/registry/models.py:
    - Session, Message tables (chat persistence)
    - Tool table (name, version, input_schema, execution_type, execution_config)
    - Prompt table (name, version, template, variables, model_hint, weight for A/B)
    - Assistant table (name, prompt_name, model_preference, tool_allowlist, resource limits)
    - Workflow table (name, steps JSONB, max_parallel_agents)
    - Evaluation table (for Phase 06, create now to avoid migration later)
  - nullrealm/registry/schemas.py: Pydantic schemas for API request/response

  Use Alembic for migrations. Create initial migration.
  Enable pgvector extension in PostgreSQL: CREATE EXTENSION IF NOT EXISTS vector;
  </action>
  <verify>alembic upgrade head && python -c "from nullrealm.registry.models import Tool, Prompt, Assistant, Workflow; print('models ok')"</verify>
  <done>All tables created via migration, models importable, pgvector extension enabled</done>
</task>

<task type="auto">
  <name>Task 2: Registry CRUD API + seed data</name>
  <files>nullrealm/api/routes/registry.py, nullrealm/registry/seed.py, agent_configs/tools/*.yaml, agent_configs/prompts/*.md, agent_configs/assistants/*.yaml</files>
  <action>
  Build CRUD API for all 4 registries:
  - nullrealm/api/routes/registry.py:
    - GET/POST/PUT/DELETE /api/v1/registry/tools
    - GET/POST/PUT/DELETE /api/v1/registry/prompts
    - GET/POST/PUT/DELETE /api/v1/registry/assistants
    - GET/POST/PUT/DELETE /api/v1/registry/workflows
    - POST /api/v1/registry/prompts/{name}/render (Jinja2 rendering)
  - Mount router in main.py

  Create seed data:
  - agent_configs/tools/: file_read.yaml, bash.yaml, web_search.yaml, grep.yaml, code_search.yaml
  - agent_configs/prompts/: research_agent.md, poc_writer.md
  - agent_configs/assistants/: research.yaml (model: gemini-pro, tools: file_read, web_search, code_search)
  - nullrealm/registry/seed.py: load YAML/MD files and insert into DB
  - scripts/seed-registries.sh: run seed script

  Do NOT add authentication to registry API yet.
  </action>
  <verify>python -m nullrealm.registry.seed && curl localhost:8000/api/v1/registry/tools | python -m json.tool</verify>
  <done>GET /api/v1/registry/tools returns 5 tools, GET /api/v1/registry/assistants returns 1 assistant</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PostgreSQL with registries and CRUD API</what-built>
  <how-to-verify>
    1. curl http://localhost:8000/api/v1/registry/tools — should return 5 seeded tools
    2. curl http://localhost:8000/api/v1/registry/prompts — should return 2 prompts
    3. curl http://localhost:8000/api/v1/registry/assistants — should return 1 assistant
    4. POST a new tool via curl and verify it persists
    5. kubectl get pods -n null-realm | grep postgres — PostgreSQL running
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All registry tables exist in PostgreSQL
- [ ] CRUD endpoints work for all 4 registries
- [ ] Seed data loaded (5 tools, 2 prompts, 1 assistant)
- [ ] pgvector extension enabled
- [ ] Alembic migration history clean
</verification>

<success_criteria>
- Full registry system operational with CRUD API
- Seed data represents real agent configurations
- Database schema matches AGENT_TECH_LAB_PLAN.md design
- Ready for agent bootstrap in Phase 04 (load config from registry)
</success_criteria>

<output>
After completion, create `.planning/phases/03-streaming-persistence/03-02-SUMMARY.md`
</output>
