---
phase: 01-foundation-observability
type: execute
---

<objective>
Set up Python project scaffold, local Kind cluster, and container build pipeline.

Purpose: Establish the project structure and local K8s environment that all subsequent phases build on.
Output: Working Kind cluster with namespaces, Dockerfiles, Makefile, and container build pipeline.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@AGENT_TECH_LAB_PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python project scaffold</name>
  <files>pyproject.toml, nullrealm/__init__.py, nullrealm/config.py, .env.example, docker-compose.yaml</files>
  <action>
  Create uv-managed Python project:
  - pyproject.toml with Python 3.14, dependencies: fastapi, uvicorn, langgraph, litellm, chainlit, sqlalchemy, asyncpg, nats-py, pydantic, opentelemetry-api
  - nullrealm/ package with __init__.py and config.py (Pydantic Settings loading from env vars)
  - .env.example documenting every required var with instructions:
    - GOOGLE_API_KEY — get from https://aistudio.google.com/apikey
    - ANTHROPIC_API_KEY — get from https://console.anthropic.com/
    - DATABASE_URL, NATS_URL, LANGFUSE_SECRET_KEY, LANGFUSE_PUBLIC_KEY, LANGFUSE_HOST
  - docker-compose.yaml as quick alternative to Kind (postgres, nats, langfuse, jaeger, grafana)
  - Use uv for dependency management (not pip/poetry)
  </action>
  <verify>cd null-realm && uv sync && python -c "import nullrealm; print('ok')"</verify>
  <done>uv sync succeeds, nullrealm package importable, .env.example documents all vars</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfiles + Makefile + Kind cluster</name>
  <files>Dockerfile.api, Dockerfile.worker, Dockerfile.ui, Makefile, scripts/kind-config.yaml, infra/k8s/base/namespace.yaml</files>
  <action>
  Create minimal Dockerfiles:
  - Dockerfile.api: Python 3.14, uv install, run uvicorn nullrealm.main:app
  - Dockerfile.worker: Python 3.14, uv install, run python -m nullrealm.worker.main
  - Dockerfile.ui: Python 3.14, pip install chainlit, run chainlit run ui/app.py

  Create Makefile with targets:
  - kind-up: create Kind cluster from scripts/kind-config.yaml, create namespaces
  - kind-down: delete Kind cluster
  - build-api / build-worker / build-ui: docker build with appropriate Dockerfile
  - load-images: kind load docker-image for all images
  - deploy-local: kubectl apply all K8s manifests
  - dev: kind-up + build + load-images + deploy-local

  Create scripts/kind-config.yaml with port mappings:
  - 8000 → API server
  - 8501 → Chainlit UI
  - 3000 → Grafana
  - 16686 → Jaeger
  - 3001 → Langfuse

  Create infra/k8s/base/namespace.yaml: null-realm + null-realm-agents namespaces
  </action>
  <verify>make kind-up && kubectl get ns | grep null-realm</verify>
  <done>Kind cluster running, both namespaces exist, Makefile targets work</done>
</task>

<task type="auto">
  <name>Task 3: Build and load container images</name>
  <files>nullrealm/main.py, ui/app.py</files>
  <action>
  Create minimal entrypoints so containers can build:
  - nullrealm/main.py: FastAPI app with GET /health returning {"status": "ok"}
  - ui/app.py: minimal Chainlit app with on_message handler that echoes input

  Build all images and load into Kind:
  - make build-api build-worker build-ui
  - make load-images

  Do NOT deploy yet — just verify images build and load.
  </action>
  <verify>docker images | grep null-realm && kind get nodes</verify>
  <done>All 3 images build successfully, loaded into Kind cluster</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv sync` succeeds in null-realm/
- [ ] Kind cluster running with `kubectl get nodes`
- [ ] Both namespaces exist: `kubectl get ns | grep null-realm`
- [ ] All 3 Docker images build: `docker images | grep null-realm`
- [ ] Makefile targets: kind-up, kind-down, build-api, build-ui work
</verification>

<success_criteria>
- Python project scaffold complete with all dependencies
- Local Kind cluster running with port mappings
- All 3 Dockerfiles build successfully
- Makefile provides single-command dev workflow
- .env.example documents every required credential
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-observability/01-01-SUMMARY.md`
</output>
