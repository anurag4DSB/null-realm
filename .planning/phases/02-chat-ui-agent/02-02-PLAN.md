---
phase: 02-chat-ui-agent
type: execute
---

<objective>
Deploy LiteLLM proxy, build first LangGraph research agent, and wire OpenLLMetry tracing to Langfuse + Jaeger.

Purpose: See a real agent think and respond — with every LLM call traced.
Output: Chat with agent in Chainlit, see traces in Langfuse (tokens, cost) and Jaeger (spans).
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/02-chat-ui-agent/02-01-SUMMARY.md
@nullrealm/main.py
@nullrealm/api/websocket.py
@nullrealm/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiteLLM Proxy deployment</name>
  <files>infra/k8s/system/litellm/deployment.yaml, infra/k8s/system/litellm/service.yaml, infra/k8s/system/litellm/litellm-config.yaml</files>
  <action>
  Deploy LiteLLM Proxy on Kind:
  - litellm-config.yaml (ConfigMap): model aliases mapping to providers
    - gemini-pro → gemini/gemini-2.5-pro (via GOOGLE_API_KEY env var)
    - claude-sonnet → anthropic/claude-sonnet-4-20250514 (via ANTHROPIC_API_KEY, optional)
  - Deployment: ghcr.io/berriai/litellm:latest, 1 replica
  - Mount config as volume, pass API keys from K8s Secret (loaded from .env)
  - Service: ClusterIP on port 4000
  - Create K8s Secret from .env file: kubectl create secret generic llm-api-keys --from-env-file=.env -n null-realm

  Start with GOOGLE_API_KEY only (user gets from https://aistudio.google.com/apikey).
  Do NOT require all API keys — LiteLLM handles missing providers gracefully.
  </action>
  <verify>kubectl get pods -n null-realm | grep litellm && kubectl exec -n null-realm deploy/api-server -- curl -s http://litellm:4000/v1/models</verify>
  <done>LiteLLM proxy running, /v1/models returns at least gemini-pro</done>
</task>

<task type="auto">
  <name>Task 2: LangGraph research agent</name>
  <files>nullrealm/worker/main.py, nullrealm/worker/langgraph_agent.py, nullrealm/worker/bootstrap.py, nullrealm/tools/base.py, nullrealm/tools/builtins/file_read.py</files>
  <action>
  Build a basic LangGraph ReAct agent:
  - nullrealm/worker/langgraph_agent.py:
    - Create LangGraph StateGraph with messages state
    - ReAct loop: LLM call → tool check → tool execute → loop or respond
    - Use ChatLiteLLM to call LiteLLM proxy (model="gemini-pro")
    - Tools: file_read (read local files)
  - nullrealm/tools/base.py: BaseTool ABC with name, description, input_schema, execute()
  - nullrealm/tools/builtins/file_read.py: read file contents given a path
  - nullrealm/worker/bootstrap.py: initialize agent with config (model, tools, system prompt)
  - nullrealm/worker/main.py: entry point that bootstraps agent and runs a single task

  Wire into API server:
  - When WebSocket receives user message, run LangGraph agent inline (not in separate pod yet)
  - Stream agent responses back through WebSocket to Chainlit

  Do NOT add NATS streaming yet — direct in-process execution for now.
  Do NOT add Argo orchestration — single agent, single process.
  </action>
  <verify>python -c "from nullrealm.worker.langgraph_agent import create_agent; a = create_agent(); print('agent created')"</verify>
  <done>Agent responds to messages via LiteLLM/Gemini, visible in Chainlit chat</done>
</task>

<task type="auto">
  <name>Task 3: Wire OpenLLMetry tracing</name>
  <files>nullrealm/observability/tracing.py, nullrealm/observability/__init__.py</files>
  <action>
  Set up OpenLLMetry to instrument LangGraph + LiteLLM calls:
  - nullrealm/observability/tracing.py:
    - Initialize OpenTelemetry with OTLP exporter (Jaeger endpoint)
    - Initialize Langfuse callback handler for LiteLLM
    - Call traceloop.init() from openllmetry for automatic LLM instrumentation
    - Export spans to both Jaeger (OTLP) and Langfuse (Langfuse SDK)
  - Import and call init_tracing() in nullrealm/main.py on startup

  Dependencies to add: opentelemetry-api, opentelemetry-sdk, opentelemetry-exporter-otlp,
  traceloop-sdk (openllmetry), langfuse

  Verify traces appear in BOTH Langfuse (LLM-specific: tokens, cost, prompt) and Jaeger (general spans).
  </action>
  <verify>Send a chat message, then check: curl localhost:16686/api/traces?service=null-realm (Jaeger) and open localhost:3001 (Langfuse)</verify>
  <done>Every LLM call shows up in Langfuse with token counts and cost, and in Jaeger with spans</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Working agent with full observability</what-built>
  <how-to-verify>
    1. Open http://localhost:8501 — Chainlit chat UI
    2. Type "What is Kubernetes?" — agent should respond with a real answer (from Gemini)
    3. Open http://localhost:3001 — Langfuse should show the trace with LLM call details (tokens, model, cost)
    4. Open http://localhost:16686 — Jaeger should show spans for the request
    5. kubectl get pods -n null-realm — litellm pod should be Running
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] LiteLLM proxy running and returning models
- [ ] Agent responds to chat messages with real LLM responses
- [ ] Langfuse shows traces with token counts and cost
- [ ] Jaeger shows distributed trace spans
- [ ] Chat flow: Chainlit → WebSocket → FastAPI → LangGraph → LiteLLM → Gemini → response
</verification>

<success_criteria>
- End-to-end chat with a real LLM agent works
- Full observability: every LLM call traced in Langfuse AND Jaeger
- LiteLLM proxy abstracts model selection (can swap models by changing config)
- Agent uses at least one tool (file_read)
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-ui-agent/02-02-SUMMARY.md`
</output>
